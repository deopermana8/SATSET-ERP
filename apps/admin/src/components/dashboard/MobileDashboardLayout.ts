import { renderBottomNavigation } from "./BottomNavigation.js";
import { renderMobileQuickAction } from "./MobileQuickAction.js";
import { renderMobileStatCard } from "./MobileStatCard.js";
import { renderMobileTaskList } from "./MobileTaskList.js";
import type { MobileDashboardProfile } from "../../app/dashboard/mobileDashboardTypes.js";

export function renderMobileDashboardLayout(profile: MobileDashboardProfile): string {
  return `<section class="view on mobile-view" id="v-mobile"><div class="mobile-shell" id="mobile-shell" data-role="${profile.role}"><header class="mobile-hero"><div class="mobile-hero-top"><span class="mobile-role-badge">${profile.badge}</span><span class="mobile-clock" id="mobile-clock">--:--</span></div><div class="mobile-hero-title">${profile.title}</div><div class="mobile-hero-greeting" id="mobile-greeting">${profile.greeting}</div><div class="mobile-hero-meta"><span class="mobile-hero-shift" id="mobile-shift-label">${profile.shiftLabel}</span><span class="mobile-hero-note" id="mobile-shift-hint">${profile.shiftHint}</span></div><div class="mobile-hero-summary">${profile.summary}</div></header><section class="mobile-panel mobile-panel-tight"><div class="mobile-panel-hd"><div><div class="mobile-panel-title">KPI</div><div class="mobile-panel-sub">Ringkasan operasional utama</div></div><span class="mobile-panel-count" id="mobile-task-count">0</span></div><div class="mobile-stat-grid">${profile.statCards.map((card) => renderMobileStatCard(card)).join('')}</div></section><section class="mobile-panel"><div class="mobile-panel-hd"><div><div class="mobile-panel-title">Shortcut Pekerjaan</div><div class="mobile-panel-sub">Akses cepat berdasarkan role</div></div><span class="mobile-panel-count">${profile.shortcuts.length}</span></div><div class="mobile-action-grid">${profile.shortcuts.map((action) => renderMobileQuickAction(action)).join('')}</div></section>${renderMobileTaskList({ title: "Task Hari Ini", badge: "Prioritas shift", items: profile.tasks })}${renderMobileTaskList({ title: "Notifikasi", badge: "Penting hari ini", items: profile.notifications })}<section class="mobile-panel"><div class="mobile-panel-hd"><div><div class="mobile-panel-title">Quick Action</div><div class="mobile-panel-sub">Tindakan harian paling sering dipakai</div></div><span class="mobile-panel-count" id="mobile-notif-count">0</span></div><div class="mobile-action-grid mobile-action-grid-compact">${profile.quickActions.map((action) => renderMobileQuickAction(action, "mobile-action-compact")).join('')}</div></section></div>${renderBottomNavigation(profile.navItems)}</section>`;
}