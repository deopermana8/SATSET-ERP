import { z } from "zod";

export const {{MODULE_CAMEL}}Schema = z.object({
  name: z.string().trim().min(1, "Nama wajib diisi").max(120, "Nama maksimal 120 karakter"),
});

export const create{{MODULE_UPPER}}Schema = {{MODULE_CAMEL}}Schema;
export const update{{MODULE_UPPER}}Schema = {{MODULE_CAMEL}}Schema.partial().refine(
  (value) => Object.keys(value).length > 0,
  { message: "Minimal satu field wajib diisi" }
);

export type {{MODULE_UPPER}}FormValues = z.infer<typeof {{MODULE_CAMEL}}Schema>;
export type Create{{MODULE_UPPER}}Input = z.infer<typeof create{{MODULE_UPPER}}Schema>;
export type Update{{MODULE_UPPER}}Input = z.infer<typeof update{{MODULE_UPPER}}Schema>;